export type StickyNoteItem = {
  id: string;
  text: string;
  createdAt: string;
  examId?: string;
  examTitle?: string;
  questionId?: string;
  questionNumber?: number;
  questionText?: string;
};
